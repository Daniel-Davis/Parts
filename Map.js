//Daniel Davis, Map, Map function that alphabetizes

load('storage.js'); // imports storage 'class'
var itemList = JSON.parse(storage); // nameList now understands storage code locically

function alphabetize(array) { // starts function alphabetize
  for(var i=0, i <= array.length), ++i) { // goes through all of the elements of array
    if ('partName' != [i]) { // if not an element
      console.log('Not a valid element'); // throws error message
      }// end of if statement
    else { // otherwise ...
      for (var i=0, i <= string.length, ++i) { // loops through the length of the name
        
        
        
      }// end of for loop
    
    
    }// end of else statement
  } // end of for loop
}// end of alphabetize



nameList.map.call(str, aplhabetize()).reverse().join(''); 
